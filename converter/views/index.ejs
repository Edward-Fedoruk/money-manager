<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>File Upload</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    input[type="file"]::-webkit-file-upload-button {
      visibility: hidden;
    }
    input[type="file"]::before {
      content: 'Choose a file';
      display: inline-block;
      background: linear-gradient(180deg, #4F46E5, #3B82F6);
      border: none;
      border-radius: 0.375rem;
      padding: 0.5rem 1rem;
      text-align: center;
      color: white;
      font-size: 1rem;
      font-weight: bold;
      cursor: pointer;
      margin-right: 1rem;
    }
    input[type="file"] {
      color: transparent;
    }
  </style>
</head>
<body class="bg-gray-100 flex justify-center items-center h-screen">
  <div class="bg-white p-8 rounded shadow-md w-full max-w-lg">
    <h1 class="text-2xl font-bold mb-4 text-center">Upload a file</h1>
    <form action="/upload" method="post" enctype="multipart/form-data" class="space-y-4">
      <label for="file-upload" class="block text-sm font-medium text-gray-700">Choose a file</label>
      <input id="file-upload" type="file" name="myFile" class="focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
      <button type="submit" class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600 w-full">Convert Private24 sheet</button>
    </form>
  </div>
</body>
</html>
